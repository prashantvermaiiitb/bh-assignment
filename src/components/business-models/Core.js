/**
 * Class for storing the basic information for the Cards.
 * id : unique identifier
 * name : name of the card will be shown on UI as well as used in the filtering process
 * images : array of images uploaded by the vendor or ingested data
 * lowerbound :
 * currency :
 */
export class Core {
  constructor(data) {
    this.id = id;
    this.name = name;
    // this.images = new Array(data.images);
    this.
  }
}
